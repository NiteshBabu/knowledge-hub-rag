fastapi
uvicorn[standard]

sqlalchemy
psycopg2-binary

pgvector

pydantic
pydantic-settings

python-multipart

pymupdf

# for local embeddings (on prod memory is ggoing out of range due to large cuda packages)
# sentence-transformers

# use gemini embedding to reduce memory usage on deployment
google-genai

httpx

python-dotenv

alembic